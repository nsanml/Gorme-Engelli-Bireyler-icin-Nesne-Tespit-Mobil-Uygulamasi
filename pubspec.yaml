name: gorme_engelli_asistan
description: Yapay zeka tabanlı nesne tespiti projesi.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  image: ^4.1.3        
  shared_preferences: ^2.2.2 
  crypto: ^3.0.3        
  flutter:
    sdk: flutter
  camera: ^0.10.5+2
  flutter_tts: ^3.8.3
  tflite_v2: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/detect.tflite
    - assets/labelmap.txt